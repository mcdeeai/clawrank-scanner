# Research Skill

Fetches data from Reddit and Brave Search APIs using subprocess calls to bird CLI.

## Setup

```bash
brew install yt-dlp
pip install requests
```

## Usage

The skill calls `bird` CLI to search X/Twitter and uses `urllib.request` to fetch Reddit data.
